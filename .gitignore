# Environment — never commit real secrets; only *.example / *.template
.env
!.env.example
docker/mikrotik/.env
!docker/mikrotik/.env.template

# Go
coverage.out
*.test
*.out

# Compiled binaries
/server
/rosmon

# AI session tools (internal planning files, not for public repo)
.sisyphus/
mikhmon/
.agents/
skills-lock.json

# Reference project — separate repo (177MB, own secrets), not part of rosmon
/billing-rtrw/
